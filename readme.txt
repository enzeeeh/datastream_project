1. docker compose  up --build
2. bikin python env di local untuk menjalankan producer dan consumer
3. kalau sudah bisa running dockernya
masuk ke spark container, untuk menjalankan processor atau via jupyter
4. run processor.py menggunakan file sh via terminal
bash ./run.sh